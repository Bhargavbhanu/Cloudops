apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: right-llm-api
  namespace: right-llm
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: right-llm-api
  minReplicas: 3
  maxReplicas: 30
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 65
