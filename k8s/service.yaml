apiVersion: v1
kind: Service
metadata:
  name: right-llm-api
  namespace: right-llm
spec:
  selector:
    app: right-llm-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
